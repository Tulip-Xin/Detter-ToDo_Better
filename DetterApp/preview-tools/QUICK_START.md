# Detter App 快速启动指�?

## 🚀 最快启动方�?

### 方式1: 使用启动脚本 (推荐)

```cmd
# 1. 检查环�?
check-environment.bat

# 2. 启动应用
start-dev.bat
```

### 方式2: 手动启动

```cmd
# 终端1: 启动Metro
npm start

# 终端2: 运行Android
npm run android
```

## 📱 准备工作

### 选项A: 使用Android模拟�?

1. 安装Android Studio
2. 创建虚拟设备 (AVD)
3. 启动模拟�?

```cmd
# 列出可用的模拟器
emulator -list-avds

# 启动模拟�?
emulator -avd Pixel_5_API_33
```

### 选项B: 使用真实设备

1. 启用开发者选项 (连续点击版本�?�?
2. 启用USB调试
3. 连接USB�?
4. 授权电脑调试

```cmd
# 检查设备连�?
adb devices
```

## 🛠�?常用命令

```cmd
# 启动开发服务器
npm start

# 运行Android应用
npm run android

# 清理缓存
npm start -- --reset-cache

# 清理Android构建
cd android && gradlew clean && cd ..

# 重新安装依赖
rm -rf node_modules && npm install

# 查看日志
adb logcat | findstr DetterApp

# 卸载应用
adb uninstall com.detterapp
```

## 🔧 开发菜�?

在模拟器/设备上：
- **打开菜单**: `Ctrl + M` (模拟�? �?摇晃设备
- **刷新**: �?`R` 两次
- **调试**: 选择 "Debug"
- **性能监控**: 选择 "Perf Monitor"

## �?热重�?

代码修改后会自动刷新，无需手动操作�?

## 📋 环境要求

- �?Node.js 16+
- �?Java JDK 11+
- �?Android SDK
- �?Android Studio (推荐)

## 🐛 常见问题

### 端口被占�?
```cmd
# 结束占用8081端口的进�?
netstat -ano | findstr :8081
taskkill /PID <PID> /F
```

### 设备未授�?
```cmd
# 撤销授权后重新连�?
adb kill-server
adb start-server
```

### 应用白屏
```cmd
# 清理缓存重新运行
npm start -- --reset-cache
```

## 📚 更多信息

- 详细指南: `WINDOWS_PREVIEW_GUIDE.md`
- 构建发布: `android/BUILD_AND_DEPLOY.md`
- 项目结构: `PROJECT_STRUCTURE.md`

## 💡 提示

1. 首次运行需要下载依赖，可能需�?-10分钟
2. 确保模拟器或设备在运行应用前已启�?
3. 使用 `check-environment.bat` 检查环境配�?
4. 遇到问题先尝试清理缓存和重新安装依赖

---

**准备好了吗？运行 `start-dev.bat` 开始开发！** 🎉
